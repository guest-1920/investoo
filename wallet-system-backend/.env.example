# ================================
# DATABASE CONFIGURATION
# ================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=your_password_here
DB_NAME=Investoo
DB_POOL_SIZE=20

# ================================
# SERVER CONFIGURATION
# ================================
PORT=3000
NODE_ENV=development

# ================================
# CORS CONFIGURATION
# ================================
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ================================
# JWT / AUTHENTICATION
# ================================
JWT_SECRET=your_secret_key_here
JWT_EXPIRES_IN=1h
JWT_COOKIE_MAX_AGE=3600000
AUTH_VERIFICATION_TOKEN_TTL=3600
AUTH_OTP_TTL=300
AUTH_RESET_TOKEN_TTL=900

# ================================
# REDIS CONFIGURATION
# ================================
REDIS_HOST=localhost
REDIS_PORT=6379

# ================================
# SCHEDULED TASKS (CRON)
# ================================
# Daily returns processing cron schedule (default: 1 AM daily)
DAILY_RETURNS_CRON=0 1 * * *

# ================================
# RATE LIMITING
# ================================
THROTTLE_TTL=60000
THROTTLE_LIMIT=60

# ================================
# EMAIL / SMTP CONFIGURATION
# ================================
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=nina.casper@ethereal.email
SMTP_PASS=dckWyzfe7vHTYU6TGU
SMTP_FROM="Investoo Support <support@Investoo.com>"
FRONTEND_URL=http://localhost:5173/

# ================================
# AWS CONFIGURATION (S3 & Textract)
# ================================
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1
AWS_BUCKET_NAME=your_bucket_name
